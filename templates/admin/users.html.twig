{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! ✅</h1>

        This friendly message is coming from:
        <ul>
            <li>Your controller at <code><a
                            href="{{ 'C:/wamp64/www/Workspaces/Symfony/test4/src/Controller/MainController.php'|file_link(0) }}">src/Controller/MainController.php</a></code>
            </li>
            <li>Your template at <code><a
                            href="{{ 'C:/wamp64/www/Workspaces/Symfony/test4/templates/admin/users.html.twig'|file_link(0) }}">templates/main/index.html.twig</a></code>
            </li>
        </ul>
    </div>

    {{ dump(users) }}

    <h2>Liste users !</h2>

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">DataTables Example</h6>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" id="dataTable">
                    <thead>
                    <tr>
                        <th>id</th>
                        <th>email</th>
                        <th>roles</th>
                        <th>est_gamer</th>
                        <th>nom</th>
                        <th>prenom</th>
                        <th>Ping !</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for user in users %}
                        <tr>
                            <td> {{ user.id }} </td>
                            <td> {{ user.email }} </td>
                            <td> {{ user.roles|json_encode }} </td>
                            <td> {{ user.isGamer }} </td>
                            <td> {{ user.nom }} </td>
                            <td> {{ user.prenom }} </td>
                            <td>
                                <form action="{{ path('admin_ping',{user : user.id}) }}" method="post">
                                    <button class="btn btn-circle btn-warning" type="submit">Envoyer un ping !</button>
                                </form>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="6">YOOT !</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    {{ dump(userbis) }}

    {{ encore_entry_script_tags('messagerie') }}
    <script type="text/javascript" charset="utf8"
            src="https://cdn.datatables.net/1.10.20/js/jquery.dataTables.js"></script>
    <script>
        // const $ = jQuery;

        // $(document).ready(function () {
        //     $('#myTable').DataTable();
        // });
    </script>
{% endblock %}
